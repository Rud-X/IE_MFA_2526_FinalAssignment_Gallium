Call Graphs
===========

Both diagrams are interactive (drag, scroll to zoom, hover a node for its
docstring summary). Generated by ``docs/build_callgraphs.py`` via static
analysis of the notebook's exported source.

Execution order
----------------

The order functions are actually invoked in the notebook's driver cell —
this is the real data-processing pipeline.

.. raw:: html

   <iframe src="_static/execution_order.html" width="100%" height="1150"
           style="border:1px solid #ccc;"></iframe>

Function call graph
--------------------

Which defined functions call other defined functions directly. The 7
functions in this notebook are independent helpers with no edges between
them — each is only called from the driver cell above, never from inside
another function — so this graph intentionally shows isolated nodes rather
than a connected graph.

.. raw:: html

   <iframe src="_static/callgraph.html" width="100%" height="720"
           style="border:1px solid #ccc;"></iframe>
